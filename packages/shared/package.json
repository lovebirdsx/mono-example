{
  "name": "@acme/shared",
  "version": "0.0.0",
  "type": "module",
  "files": ["dist"],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build",
    "test": "vitest run",
    "dev": "tsc --build --watch",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@acme/config-ts": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
